part of "../../global/widgets.dart";

class CheckTokenWidget extends StatelessWidget {
  const CheckTokenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}